interface:
  display_name: "HireData Linear Issues"
  short_description: "Draft clear, actionable HireData issue reports"
  default_prompt: "Use $hiredata-create-linear-issues to turn this evidence into a HireData-compatible issue report. Draft only unless I explicitly approve a live change."
